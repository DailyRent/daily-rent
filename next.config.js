/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
    defaultLocale: 'ua',
    locales: ['default', 'ua', 'en'],
    localeDetection: false,
    }
}

module.exports = nextConfig
