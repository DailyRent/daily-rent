import React from "react";
// import styles from "./Hero.module.scss";

const Hero = () => {
  return (
    <section
      style={{
        backgroundColor: "#ddd",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
      }}
    >
      Hero
    </section>
  );
};

export default Hero;
